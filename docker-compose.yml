services:
  frontend:
    build:
      context: ./monorepo_front
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - backend

  backend:
    build:
      context: ./monorepo_back
      dockerfile: Dockerfile
    ports:
      - "8080:8080"